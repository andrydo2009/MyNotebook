 public boolean appearsIn (LocalDate){};
 метод который пока он тру понимает что задача актуальна
 то есть он сравнивает текущую дату с датой создания задачи то есть ее актуальность
 по идее в логике метода должно быть такая есть некая actualDate которая сравнивается
 с датой создания





         ctrl + b найти использование методов полей и прочее

         alt+enter для импортов

         если значение не передано в конструктор оно устанавливается по умолчанию(null)
         переименование всех переменных рефакторинг правой кнопкой


